<!-- _cart.html.erb -->

<% if !current_cart.empty?%>
	<div>
		<table class="table">
			<thead>
				<tr>
					<th>Food</th>
					<th>Amount</th>
					<th>Price</th>
				</tr>
			</thead>
			<tbody>
				<% current_cart.items.each do |cart_item|%>
				<tr>
					<td><%= cart_item.item.name %> ( $<%= cart_item.item.price %> )</td>
					<td><%= cart_item.quantity %></td>
					<td><%= cart_item.total %></td>
				</tr>
				<% end %>
			</tbody>
			<tfoot>
				<tr>
					<td colspan="2">Total</td>
					<td><%= current_cart.total %></td>
				</tr>
			</tfoot>
		</table>
	</div>
<% end %>