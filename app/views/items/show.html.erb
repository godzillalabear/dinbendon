<h1><%= @item.name %></h1>

<ul>
  <li>Price：<%= @item.price %></li>
</ul>


<h3>Comment</h3>
<%= form_for(@comment, url:item_comments_path(@item)) do |form|%>
	<div>
		<%= form.text_area :content%>
	</div>
	<%= form.submit %>
<%end %>