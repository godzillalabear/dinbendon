<h1>餐點列表</h1>

<%= link_to "Add new", new_item_path %>

<ul>
  <% @items.each do |item| %>
    <li>
      <%= link_to item.name, item_path(item.id) %>
      <%= link_to "Delete", item_path(item.id), method: 'delete', data: {confirm: "Do you really want to delete?"} %>
    </li>
  <% end %>
</ul>

